"""External integrations for the MindForge Coach Admin Streamlit app.

Modules:
    auth             — geteilter Passwort-Gate für app.py und pages/*
    airtable_helpers — Hot-Lead-Reader + Booking-Writer (pyairtable)
    google_calendar  — Google Calendar API via Service Account (Meet-Links)
    aircall          — Click-to-Call (Stub — siehe aircall.py.stub)
"""
